# Database
# In production the app prefers PRISMA_DATABASE_URL (the pooled
# prisma+postgres:// URL from the Prisma Postgres integration). The direct
# TCP URL's role has a small connection cap meant for migrations/CLI use.
DATABASE_URL="postgresql://username:password@localhost:5432/auto_glass_platform?schema=public"
DIRECT_URL="postgresql://username:password@localhost:5432/auto_glass_platform?schema=public"
# PRISMA_DATABASE_URL="prisma+postgres://..."

# Subdomain provisioning (admin "Connect" button: Cloudflare CNAME +
# Vercel domain attach). Create the Cloudflare token with Zone → DNS → Edit
# scoped to glassleads.app; the Zone ID is on the domain's overview page.
# Create the Vercel token under Account Settings → Tokens.
CLOUDFLARE_API_TOKEN="your-cloudflare-dns-edit-token"
CLOUDFLARE_ZONE_ID="your-glassleads-app-zone-id"
VERCEL_TOKEN="your-vercel-account-token"

# Authentication
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Call Coaching (Deepgram transcription + Claude analysis)
ANTHROPIC_API_KEY="sk-ant-..."
DEEPGRAM_API_KEY="your-deepgram-key"
# HighLevel webhook secret used by both /lead and /call-recording endpoints
HIGHLEVEL_WEBHOOK_SECRET="shared-secret"
# Optional: explicit base URL used when the webhook fires the internal worker.
# If unset, falls back to VERCEL_URL or the request host.
APP_URL="https://glassleads.app"

# Email Notifications
ADMIN_EMAIL="matt.lubbes@gmail.com"

# Master Leads Page Access (email allowed to view all clients' leads)
MASTER_LEADS_EMAIL="matt.lubbes@gmail.com"

# Security
ENCRYPTION_KEY="generate-with-openssl-rand-hex-32"
CRON_SECRET="generate-with-openssl-rand-base64-32"

# Google Places (business lookup / address autocomplete in the admin client
# form). Can also be stored via the admin settings page instead of env.
GOOGLE_PLACES_API_KEY="your-google-places-key"

# Web Push Notifications (VAPID keys)
NEXT_PUBLIC_VAPID_PUBLIC_KEY="your-vapid-public-key"
VAPID_PRIVATE_KEY="your-vapid-private-key"
